.location-form {
  .menu {
    li, a {
      width: auto;
      display: block;
    }
  }
  input {
    width: auto;
    display: block;
  }
}

label, p {
  display: block;
  margin: 1em;
}

.invalid_input {
  border: 1px solid #f00;
}
