import React from 'react';
import ErrorMessage from '../error-message';

const PageNotFound = () => (
  <ErrorMessage>Page Not Found. <a href="/">Go back home.</a></ErrorMessage>
);

export default PageNotFound;
